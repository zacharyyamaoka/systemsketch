{
  "host": "Cursor",
  "editor": "systemsketch.editor",
  "passed": 8,
  "checks": [
    "a blank target.systemsketch opens as a SystemSketch canvas, not a quarantine screen",
    "the pane hosts the real SystemSketch toolbar, Block tool included",
    "the in-app file menu and share shell are absent; the board surfaces remain",
    "the workbench theme reaches the board — .monaco-workbench paints rgb(37, 37, 38), the canvas records \"dark\", tldraw paints dark (rgb(24, 24, 24) = the workbench's editor.background #181818), and the chrome wears the \"vscode\" theme",
    "a canvas edit marks the tab dirty and writes nothing until the IDE saves",
    "the inspector is legible in the dark workbench — section-title 8.3:1, field > span 8.3:1, tabs > [role=\"tab\"].is-active 14.82:1",
    "Ctrl+S writes a .systemsketch: a tldraw file behind one SystemSketch envelope",
    "reopening the saved file redraws the Block that was saved"
  ],
  "blocked": "Cursor is showing its first-run sign-in wall over the workbench",
  "uncovered": [
    ".tldr saved without an envelope",
    "external JSON edit reloads the canvas"
  ],
  "note": "Signing in is the user's own account action, so the suite stops here. Everything above was driven behind that wall in this host; run the suite against VS Code, or a signed-in Cursor profile, for the remaining two.",
  "captures": [
    "docs/assets/cursor-target-open.png",
    "docs/assets/cursor-target-block-saved.png"
  ]
}
